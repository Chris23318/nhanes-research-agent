const test=require('node:test'),assert=require('node:assert/strict');
const {createModelSpec}=require('../src/model-spec');
const {generateModelScript}=require('../src/model-script');
function project(){const cycles=['2017-2018'];return {status:'awaiting_approval',intent:{cycles},cleaningApproval:{digest:'clean'},variables:[{role:'exposure',variable:'LBXBPB',sourceFile:'PBCD_J',cycles,confirmationStatus:'codebook_and_cleaning_approved'},{role:'outcome',variable:'BPXSY1',sourceFile:'BPX_J',cycles,confirmationStatus:'codebook_and_cleaning_approved'},{role:'covariate',variable:'RIDAGEYR',cycles,confirmationStatus:'codebook_and_cleaning_approved'},{role:'covariate',variable:'RIAGENDR',cycles,confirmationStatus:'codebook_and_cleaning_approved'},{role:'design',variable:'WTMEC2YR',cycles},{role:'design',variable:'SDMVSTRA',cycles},{role:'design',variable:'SDMVPSU',cycles}]}}
const input={outcomeFamily:'continuous',exposureTransform:'log2',outcomeTransform:'raw',weightVariable:'WTMEC2YR',covariates:[{variable:'RIDAGEYR',encoding:'continuous'},{variable:'RIAGENDR',encoding:'factor'}],acknowledgeAssociationOnly:true};
test('approved model spec freezes cycle mappings and explicit encodings',()=>{const spec=createModelSpec(project(),input);assert.equal(spec.status,'approved_for_code_generation_not_execution');assert.equal(spec.covariates[1].encoding,'factor');assert.equal(spec.digest.length,64)});
test('generated model uses survey design, a valid formula, transformations and finite-result gates',()=>{const script=generateModelScript(createModelSpec(project(),input));assert.match(script,/svydesign/);assert.match(script,/svyglm\(analysis_outcome ~ analysis_exposure/);assert.match(script,/log2/);assert.match(script,/factor/);assert.match(script,/Nonfinite model result/);assert.doesNotMatch(script,/\.data\[\[/)});
test('model approval rejects unconfirmed mappings and causal overclaim',()=>{const p=project();p.variables=p.variables.filter(x=>x.role!=='outcome');assert.throws(()=>createModelSpec(p,input),/结局/);assert.throws(()=>createModelSpec(project(),{...input,acknowledgeAssociationOnly:false}),/关联/)});
